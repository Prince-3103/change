Drop your own files here, using these exact names:

  profile.jpg    → the circular birthday portrait (square photo works best)
  birthday.png   → small illustration/sticker at the top of the letter
  birthday.mp3   → optional background music
  blow.mp3       → optional short "candle extinguish" sound effect

None of these files exist yet — that's expected. The site already
handles missing assets gracefully:
  - profile.jpg missing  → shows a 🎂 fallback in the circle
  - birthday.png missing → shows a 🎂 fallback above the letter
  - birthday.mp3 missing → the music button just won't produce sound
  - blow.mp3 missing     → candles still blow out silently

Just add real files with the names above and everything lights up
automatically — no code changes needed.
